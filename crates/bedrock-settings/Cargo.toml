[package]
name = "bedrock-settings"
description = "Project Bedrock persistent settings — TOML preferences in the user config directory."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
dirs = { workspace = true }
tracing = { workspace = true }
